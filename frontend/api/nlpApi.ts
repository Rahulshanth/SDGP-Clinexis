/*import axios from "axios";

const API_BASE_URL = "http://localhost:5001";

export const extractMedicines = async (text: string) => {
  const response = await axios.post(`${API_BASE_URL}/nlp/extract`, {
    text: text,
  });

  return response.data;
};*/